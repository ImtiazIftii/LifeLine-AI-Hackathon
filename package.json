{
  "name": "lifeline-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:backend": "python -m uvicorn app.main:app --app-dir backend --host 0.0.0.0 --port 4000 --reload",
    "dev:frontend": "npm --prefix frontend run dev",
    "install:all": "pip install -r backend/requirements.txt && npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "check": "python -m py_compile backend/app/*.py && npm --prefix frontend run build",
    "start": "docker compose up"
  }
}
